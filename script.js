function clicar() {
    let number1 = document.getElementById('number1')
    if (number1.value < 1 || number.value > 100 || number1.value.length == 0) {
        alert('Não foi possível efetuar essa operação!')
    } else {
        let listaNumeros = [ ]
        number.value.push()
    }
}